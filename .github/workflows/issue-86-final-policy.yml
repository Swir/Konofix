name: Issue 86 final public-host policy integration

on:
  push:
    branches:
      - hardening/public-host-global-policy-86

permissions:
  contents: write

concurrency:
  group: issue-86-final-policy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  integrate:
    if: ${{ !contains(github.event.head_commit.message, '[issue-86-finalized]') }}
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Checkout integration branch
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: true
          fetch-depth: 0

      - name: Normalize checkout line endings
        shell: bash
        run: |
          set -euo pipefail
          git config core.autocrlf false
          git reset --hard HEAD

      - name: Apply final fail-closed policy patch
        shell: bash
        run: |
          set -euo pipefail
          python3 .github/issue-86-finalize.py
          python3 .github/issue-86-extra.py

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@6bed0761d98439e5a578e2877258200ad565ba87
        with:
          toolchain: stable
          components: rustfmt, clippy

      - name: Format Rust patch
        run: cargo fmt --manifest-path src-tauri/Cargo.toml --all

      - name: Install frontend dependencies
        run: npm ci --no-audit --no-fund

      - name: Project audit including adversarial public-host policy
        run: npm run audit

      - name: Linux public-node deployment self-tests
        run: bash scripts/test-public-node-linux.sh

      - name: TypeScript and Vite build
        run: npm run build

      - name: Rust format check
        run: |
          cargo fmt --manifest-path src-tauri/Cargo.toml --all -- --check
          cargo fmt --manifest-path node-linux/Cargo.toml -- --check

      - name: Prepare isolated Node lockfile
        run: cp src-tauri/Cargo.lock node-linux/Cargo.lock

      - name: Raw Node unit tests
        run: cargo test --manifest-path node-linux/Cargo.toml --locked --bin konofix-node

      - name: Raw Node Clippy
        run: cargo clippy --manifest-path node-linux/Cargo.toml --locked --bin konofix-node -- -D warnings

      - name: Patch hygiene
        run: git diff --check

      - name: Commit verified patch and remove integration helpers
        shell: bash
        run: |
          set -euo pipefail
          git rm .github/issue-86-finalize.py .github/issue-86-extra.py .github/workflows/issue-86-final-policy.yml
          git diff --check
          git add -A
          if git diff --cached --quiet; then
            echo 'Final integration produced no changes.' >&2
            exit 1
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'Complete fail-closed public-host special-use policy [issue-86-finalized]'
          git push origin HEAD:${GITHUB_REF_NAME}
