{
  "name": "konofix-chat",
  "private": true,
  "version": "0.4.2",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "audit": "node scripts/project-audit.mjs && node scripts/check-dependabot-config.mjs && node scripts/check-typescript-compat.mjs && node scripts/test-readme-standard.mjs && node scripts/check-readme-standard.mjs && node scripts/check-local-ci-parity.mjs",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.0",
    "typescript": "^7.0.2",
    "vite": "^8.3.0"
  }
}
